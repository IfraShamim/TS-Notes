// Map & Filter Method in Array In TS
const numbers:number[] = [1,2,3,4,5];

// Map Method ----------

// Doubling Each Number **********

// const doubleData = numbers.map((num)=>{
//     console.log(num*2); 
// })
// console.log(doubleData);

// Converting numbers to string *******
//  const convertString = numbers.map((currValue) => currValue.toString())
//  console.log(convertString);
 

// Filter Method ------------
// Filtering Even Numbers********
// const numb = numbers.filter((num) => num % 2 === 0);
// console.log(numb);

// Filtering Numbers Greater Than 3 *********
// const numb = numbers.filter((num) => num > 3);
// console.log(numb);